% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GREG.SI.r
\name{GREG.SI}
\alias{GREG.SI}
\title{Generalised Regression Estimator under Simple Random Sampling}
\usage{
GREG.SI(N, n, y, x, tx, b, b0 = FALSE)
}
\arguments{
\item{N}{Population size.}

\item{n}{Sample size.}

\item{y}{Vector, matrix or data frame of variables of interest.}

\item{x}{Vector, matrix or data frame of auxiliary variables observed
in the sample.}

\item{tx}{Vector of known population totals for the auxiliary variables.}

\item{b}{Matrix of regression coefficients (e.g. from \code{\link{E.Beta}}).}

\item{b0}{Logical. If \code{TRUE}, an intercept column is prepended to
\code{x}. Default is \code{FALSE}.}
}
\value{
A matrix with three rows and one column per variable of interest:
\itemize{
  \item \code{Estimation}: GREG estimated population total.
  \item \code{Standard Error}: Estimated standard error.
  \item \code{CVE}: Estimated coefficient of variation (in percentage).
}
}
\description{
Computes the Generalised Regression (GREG) estimator of the population
total under simple random sampling without replacement, using known
population totals of auxiliary variables to improve efficiency.
}
\details{
The GREG estimator is:
\deqn{\hat{t}_{GREG} = \hat{t}_{HT} + (\mathbf{t}_x -
\hat{\mathbf{t}}_{x,HT})^T \hat{\boldsymbol{\beta}}}
where \eqn{\hat{\boldsymbol{\beta}}} are the regression coefficients
estimated from the sample, \eqn{\mathbf{t}_x} are the known population
totals, and variance is estimated from the residuals.
}
\examples{
data('Lucy')
attach(Lucy)
N   <- nrow(Lucy)
n   <- 400
sam <- S.SI(N, n)
y   <- data.frame(Income = Income[sam])
x   <- data.frame(Employees = Employees[sam])
tx  <- sum(Employees)
b   <- E.Beta(N, n, y, x, b0 = FALSE)
GREG.SI(N, n, y, x, tx, b)
}
\references{
Sarndal, C-E. and Swensson, B. and Wretman, J. (1992),
\emph{Model Assisted Survey Sampling}. Springer.\cr
Gutierrez, H. A. (2009), \emph{Estrategias de muestreo: Diseno de encuestas
y estimacion de parametros}. Editorial Universidad Santo Tomas.
}
\seealso{
\code{\link{E.Beta}}, \code{\link{E.SI}}, \code{\link{Wk}}
}
\author{
Hugo Andres Gutierrez Rojas <hagutierrezro at gmail.com>
}
